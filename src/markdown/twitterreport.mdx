import React from 'react';
import ReactMarkdown from 'react-markdown';

export default function MyMDXFile({ apiData }) {
  return (
    <div>
      {apiData ? (
        <ReactMarkdown>{apiData}</ReactMarkdown> 
      ) : (
        <p>Loading...</p>
      )}
    </div>
  );
}
